mdp

const lim = 100;

module value
  value : [1 .. lim] init 1;
  [forward] true -> 1/2 : (value'=min(lim,value+1)) + 1/2 : (value'=value);
  [run] true -> 9/10 : (value'=min(lim,value + 5)) + 1/10 : (value'=1);
endmodule

label "goal" = value=lim;
